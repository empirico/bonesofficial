<h2><? echo $this->portfolio->getTitle() ?></h2>
<p><? echo $this->portfolio->getDescription() ?></p>
<div class="work-item-container">
<? foreach ($this->work_list as $album) {
        //$album = new Album();
        $cover = $album->getCoverPhoto();
        $image = $cover->getImage();
        $path = ($image) ? $image->getFullThumbPath(WorksController::DEFAULT_THUMB) : "/images/admin/default_album_cover.jpg"
        ?>
        <div class="work-item">
            <div class="work-item-thumb" style="background: url('<? echo $path ?>') top center">
                <a href="<?php echo $this->url(array('controller' => 'works', 'action' => 'show', 'slug' => $album->getSluggedIndex(), 'lang' => $this->lang), 'works', true); ?>"><img src="/images/frame.png" width="200" height="200" align="absmiddle" border="0"/></a>
            </div>
            <div class="work-item-title"><?php echo $album->getTitle(); ?></div>
        </div>
<?php } ?>
    <div class="clear"></div>
</div>